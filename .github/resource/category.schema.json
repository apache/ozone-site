{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ozone.apache.org/category.schema.json",
  "title": "Category",
  "description": "Validates an allowed subset of keys in Docusaurus _category_.yml files.",
  "type": "object",
  "properties": {
    "label": {
      "description": "The name of this section shown in the docs sidebar.",
      "type": "string"
    },
    "link": {
      "description": "Determines the behavior of the link created for this section.",
      "type": "object",
      "properties": {
        "description": {
          "description": "A short description of this section's content.",
          "type": "string"
        },
        "type": {
          "description": "The type of page to generate for this section.",
          "type": "string",
          "enum": ["generated-index"]
        }
      },
      "required": ["description", "type"],
      "additionalProperties": false
    }
  },
  "required": [ "label", "link" ],
  "additionalProperties": false
}
